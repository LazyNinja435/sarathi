# Ramayana (planned)

Reserved for a future public-domain English path (e.g. Griffith, verified PD editions) with the same `raw/` → `processed/` → `indexes/` flow.

**TODO:** Add `source_manifest.json`, register in `tools/rag-builder/config/sources.yaml`, and implement `normalize_ramayana.py` when ready.
